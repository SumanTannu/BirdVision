"""Command-line entry points for bird classifier training and inference.

The `scripts` package exists so the root launcher file can import
`scripts.train.main` reliably. Users usually run these modules directly from the
command line rather than importing them.
"""
