"""Bird classification package using EfficientNetB0.

The package is split into small modules for configuration, dataset loading,
model construction, callbacks, evaluation, and visualization. The command-line
scripts compose these modules into train, evaluate, and predict workflows.
"""
